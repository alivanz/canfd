diff -uNr old/mcp251x/mcp251x.c new/mcp251x/mcp251x.c
--- old/mcp251x/mcp251x.c	2024-07-02 10:49:18.000000000 +0800
+++ new/mcp251x/mcp251x.c	2024-11-07 11:43:24.241787173 +0800
@@ -500,7 +500,7 @@
 	priv->net->stats.rx_packets++;
 	priv->net->stats.rx_bytes += frame->can_dlc;
 
-#if KERNEL_VERSION(5, 18, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 	netif_rx(skb);
 #else
 	netif_rx_ni(skb);
@@ -728,7 +728,7 @@
 	if (skb) {
 		frame->can_id |= can_id;
 		frame->data[1] = data1;
-#if KERNEL_VERSION(5, 18, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 		netif_rx(skb);
 #else
 		netif_rx_ni(skb);
diff -uNr old/mcp251xfd/mcp251xfd-core.c new/mcp251xfd/mcp251xfd-core.c
--- old/mcp251xfd/mcp251xfd-core.c	2024-07-02 10:49:24.000000000 +0800
+++ new/mcp251xfd/mcp251xfd-core.c	2024-11-07 11:49:18.602542196 +0800
@@ -1542,7 +1542,7 @@
 	mcp251xfd_hw_rx_obj_to_skb(priv, hw_rx_obj, skb);
 
 	/* Change by SUNIX */
-#if KERNEL_VERSION(5, 19, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 	err = can_rx_offload_queue_timestamp(&priv->offload, skb, hw_rx_obj->ts);
 #elif KERNEL_VERSION(4, 20, 0) <= LINUX_VERSION_CODE
 	err = can_rx_offload_queue_sorted(&priv->offload, skb, hw_rx_obj->ts);
@@ -1690,7 +1690,7 @@
 	cf->data[1] = CAN_ERR_CRTL_RX_OVERFLOW;
 
 	/* Change by SUNIX */
-#if KERNEL_VERSION(5, 19, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 	err = can_rx_offload_queue_timestamp(&priv->offload, skb, timestamp);
 #elif KERNEL_VERSION(4, 20, 0) <= LINUX_VERSION_CODE
 	err = can_rx_offload_queue_sorted(&priv->offload, skb, timestamp);
@@ -1801,7 +1801,7 @@
 		return 0;
 
 	/* Change by SUNIX */
-#if KERNEL_VERSION(5, 19, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 	err = can_rx_offload_queue_timestamp(&priv->offload, skb, timestamp);
 #elif KERNEL_VERSION(4, 20, 0) <= LINUX_VERSION_CODE
 	err = can_rx_offload_queue_sorted(&priv->offload, skb, timestamp);
@@ -1883,7 +1883,7 @@
 	}
 
 	/* Change by SUNIX */
-#if KERNEL_VERSION(5, 19, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 	err = can_rx_offload_queue_timestamp(&priv->offload, skb, timestamp);
 #elif KERNEL_VERSION(4, 20, 0) <= LINUX_VERSION_CODE
 	err = can_rx_offload_queue_sorted(&priv->offload, skb, timestamp);
@@ -2233,7 +2233,7 @@
 
 		if (!(intf_pending)) {
 			/* Add by SUNIX */
-#if KERNEL_VERSION(5, 15, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 			can_rx_offload_threaded_irq_finish(&priv->offload);
 #endif
 			return handled;
@@ -2338,7 +2338,7 @@
 
 out_fail:
 	/* Add by SUNIX */
-#if KERNEL_VERSION(5, 15, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 	can_rx_offload_threaded_irq_finish(&priv->offload);
 #endif
 
diff -uNr old/sdccanfd/sdc_canfd.c new/sdccanfd/sdc_canfd.c
--- old/sdccanfd/sdc_canfd.c	2024-07-02 10:49:45.000000000 +0800
+++ new/sdccanfd/sdc_canfd.c	2024-11-07 11:54:34.636236478 +0800
@@ -1126,7 +1126,7 @@
 	}
 
 	/* can rx offload queue work */
-#if (KERNEL_VERSION(5, 19, 0) <= LINUX_VERSION_CODE)
+#if (KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE)
 	err = can_rx_offload_queue_timestamp(&priv->offload, skb, timestamp);
 #elif ((KERNEL_VERSION(4, 20, 0) <= LINUX_VERSION_CODE) && (KERNEL_VERSION(5, 19, 0) > LINUX_VERSION_CODE))
 	err = can_rx_offload_queue_sorted(&priv->offload, skb, timestamp);
@@ -1199,7 +1199,7 @@
 	cf->data[1] = error_status;
 
 	/* can rx offload queue work */
-#if (KERNEL_VERSION(5, 19, 0) <= LINUX_VERSION_CODE)
+#if (KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE)
 	err = can_rx_offload_queue_timestamp(&priv->offload, skb, timestamp);
 #elif ((KERNEL_VERSION(4, 20, 0) <= LINUX_VERSION_CODE) && (KERNEL_VERSION(5, 19, 0) > LINUX_VERSION_CODE))
 	err = can_rx_offload_queue_sorted(&priv->offload, skb, timestamp);
@@ -1336,7 +1336,7 @@
 	}
 
 	/* can rx offload queue work */
-#if (KERNEL_VERSION(5, 19, 0) <= LINUX_VERSION_CODE)
+#if (KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE)
 	err = can_rx_offload_queue_timestamp(&priv->offload, skb, timestamp);
 #elif ((KERNEL_VERSION(4, 20, 0) <= LINUX_VERSION_CODE) && (KERNEL_VERSION(5, 19, 0) > LINUX_VERSION_CODE))
 	err = can_rx_offload_queue_sorted(&priv->offload, skb, timestamp);
@@ -1777,7 +1777,7 @@
 
 
 	/* can rx offload queue work */
-#if (KERNEL_VERSION(5, 19, 0) <= LINUX_VERSION_CODE)
+#if (KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE)
 	err = can_rx_offload_queue_timestamp(&priv->offload, skb, rx_obj->ts);
 #elif ((KERNEL_VERSION(4, 20, 0) <= LINUX_VERSION_CODE) && (KERNEL_VERSION(5, 19, 0) > LINUX_VERSION_CODE))
 	err = can_rx_offload_queue_sorted(&priv->offload, skb, rx_obj->ts);
@@ -1892,7 +1892,7 @@
 #endif
 
 	if (event == 0 || event == 1) {
-#if KERNEL_VERSION(5, 15, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 		can_rx_offload_threaded_irq_finish(&priv->offload);
 #endif
 		return handled;
@@ -1934,7 +1934,7 @@
 #endif
 
 		if (!(intf_pending)) {
-#if KERNEL_VERSION(5, 15, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 			can_rx_offload_threaded_irq_finish(&priv->offload);
 #endif
 
@@ -2094,7 +2094,7 @@
 	} while (1);
 
 out_fail:
-#if KERNEL_VERSION(5, 15, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 	can_rx_offload_threaded_irq_finish(&priv->offload);
 #endif
 
diff -uNr old/sdc_config.c new/sdc_config.c
--- old/sdc_config.c	2024-07-15 11:38:19.000000000 +0800
+++ new/sdc_config.c	2024-11-07 11:40:36.040871209 +0800
@@ -525,7 +525,7 @@
 }
 #endif
 
-#if KERNEL_VERSION(6, 2, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 static int sdc_config_uevent(const struct device *dev, struct kobj_uevent_env *env)
 #else
 static int sdc_config_uevent(struct device *dev, struct kobj_uevent_env *env)
@@ -690,7 +690,7 @@
 		pr_warn("failed to create driver proc entry\n");
 #endif
 
-#if KERNEL_VERSION(6, 4, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 	dev_class = class_create(DRV_NAME);
 #else
 	dev_class = class_create(THIS_MODULE, DRV_NAME);
diff -uNr old/sdc_dio.c new/sdc_dio.c
--- old/sdc_dio.c	2024-07-30 10:09:16.000000000 +0800
+++ new/sdc_dio.c	2024-11-07 11:41:39.246838651 +0800
@@ -956,7 +956,7 @@
 }
 #endif
 
-#if KERNEL_VERSION(6, 2, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 static int sdc_dio_uevent(const struct device *dev, struct kobj_uevent_env *env)
 #else
 static int sdc_dio_uevent(struct device *dev, struct kobj_uevent_env *env)
@@ -1146,7 +1146,7 @@
 		pr_warn("failed to create driver proc entry\n");
 #endif
 
-#if KERNEL_VERSION(6, 4, 0) <= LINUX_VERSION_CODE
+#if KERNEL_VERSION(5, 14, 0) <= LINUX_VERSION_CODE
 	dev_class = class_create(DRV_NAME);
 #else
 	dev_class = class_create(THIS_MODULE, DRV_NAME);
